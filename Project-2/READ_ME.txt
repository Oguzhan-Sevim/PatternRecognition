

---- Just run main.m. The precision and recall values will be on command window.

---- PCA function is called in the main.m file.

---- You can change the d' value in line 3 of main.m file. Selecting d_prime higher than 80 causes the covariance matrix to be singular.
